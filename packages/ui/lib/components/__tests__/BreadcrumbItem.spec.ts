import { describe, it } from 'vitest';
// import { mount } from '@vue/test-utils';
// import BreadcrumbItem from '../BreadcrumbItem.vue';

describe('', () => {
    it('', () => {
        // const wrapper = mount(BreadcrumbItem, { props: { text: 'Word'}});
    });
})